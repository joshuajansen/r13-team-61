<article class="page" style="background-image: url(<%= page.background_image.url(:retina) %>);">
  <% markdown = Redcarpet::Markdown.new(Redcarpet::Render::HTML) %>
  <%= markdown.render(page.body || "").html_safe %>
</article>